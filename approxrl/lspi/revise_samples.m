function samples = revise_samples(samples)
% Revise older samples structures to conform to new implementation details
% Should be called whenever loading samples objects from files

% do nothing for now